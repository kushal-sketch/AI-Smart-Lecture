# Copy this file to ".env" and fill in real values.
# ".env" is gitignored and never committed.

# Flask
SECRET_KEY=change-this-to-a-long-random-string
FLASK_DEBUG=1

# Optional: real AI instead of mock fallbacks
GEMINI_API_KEY=
WHISPER_MODEL_SIZE=base
