// dashboard.js — reserved for future chart/analytics interactivity
document.addEventListener("DOMContentLoaded", () => {
  // Stat tiles already render server-side; this file is a hook point
  // for adding charts (e.g. Chart.js) once enough lecture history exists.
});
